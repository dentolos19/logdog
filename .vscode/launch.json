{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Start",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}",
      "command": "just start",
      "preLaunchTask": "Setup",
      "serverReadyAction": {
        "pattern": "Local:.+(https?://.+)",
        "uriFormat": "%s",
        "action": "openExternally"
      }
    }
  ]
}
