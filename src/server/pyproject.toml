[project]
name = "naisc-server"
version = "1.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
  "bcrypt",
  "boto3",
  "fastapi[standard]",
  "pydantic>=2.0.0,<2.12",
  "psycopg2-binary",
  "python-dotenv",
  "python-jose[cryptography]",
  "sqlalchemy",
  "uvicorn>=0.41.0",
]
