[project]
name = "naisc-server"
version = "1.0.0"
requires-python = ">=3.12,<3.14"
dependencies = [
  "bcrypt",
  "boto3",
  "fastapi[standard]",
  "psycopg2-binary",
  "python-dotenv",
  "python-jose[cryptography]",
  "sqlalchemy",
]
